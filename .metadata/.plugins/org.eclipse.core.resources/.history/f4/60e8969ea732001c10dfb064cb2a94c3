package com.maxtrain.practiceWithJava;

public class JavaPractice {

	public static void main(String[] args) {
	
		
		for(int idx = 0; idx <=75; idx++) {
			if(calcModulo(idx, 3) == 0 && calcModulo(idx, 7 == 0));
			if(calcModulo(idx, 3) == 0);
			if(calcModulo(idx, 7) == 0);
		
				System.out.println(idx + ", ");
		}
	}

	private static boolean calcModulo(int idx, boolean b) {
		// TODO Auto-generated method stub
		return false;
	}

	private static int calcModulo(int idx, int i) {
		// TODO Auto-generated method stub
		return 0;
	}

}
